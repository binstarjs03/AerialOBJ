<UserControl x:Class="binstarjs03.AerialOBJ.WpfAppNew2.Views.PropertyValuePairView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:binstarjs03.AerialOBJ.WpfAppNew2.Views"
             mc:Ignorable="d"
             x:Name="Root">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding ElementName=Root, Path=PropertyText}" Foreground="{Binding ElementName=Root, Path=PropertyColor}"/>
            <TextBlock Text=": " Foreground="{Binding ElementName=Root, Path=PropertyColor}"/>
            <TextBlock Text="{Binding ElementName=Root, Path=ValueText}" Foreground="{Binding ElementName=Root, Path=ValueColor}"/>
        </StackPanel>
    </Grid>
</UserControl>
