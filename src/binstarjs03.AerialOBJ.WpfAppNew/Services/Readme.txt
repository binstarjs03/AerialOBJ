Service API are static, self-contained, and dependency-free API. 
Therefore, any API in this folder MUST conforms these specifications.

The only exception for service API is having dependency on SharedStateService.